## What this changes

<!-- One or two sentences. If it fixes an issue, link it. -->

## How it was checked

<!-- `npm test`, plus what you tried against a real design: which node, which tool, what came back. -->

## Notes

- [ ] `npm test` passes
- [ ] New raw Figma fields are handled defensively (missing field means the key is left out)
- [ ] No `console.log` anywhere in `src/` — stdout carries the MCP protocol
